<UserControl x:Class="AdminWPFClient.Content.StudentsInfo"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AdminWPFClient.Content"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel>
            <ListBox ItemsSource="{Binding Path=Students}"
                      DisplayMemberPath="Name"
                     SelectedItem="{Binding Path=SelectedStydent, Mode=TwoWay}"/>
        </StackPanel>
        <GridSplitter  Grid.Column="1" Opacity="0"></GridSplitter>
        <StackPanel Grid.Column="2">
            <TextBlock Text="{Binding Path=SelectedStydent.Name}" Style="{StaticResource Heading2}" Margin="0,0,0,8"/>
            <ListBox ItemsSource="{Binding Path=SelectedStydentTests}"
                      DisplayMemberPath="Name"/>
        </StackPanel>
    </Grid>
</UserControl>
