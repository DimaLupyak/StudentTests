<UserControl x:Class="AdminWPFClient.Content.LoadingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <ProgressBar DockPanel.Dock="Top" Minimum="0" Maximum="1"  IsIndeterminate="True"  Height="30"/>
        <TextBlock DockPanel.Dock="Top" Text="Загрузка даних" 
            HorizontalAlignment="Center" FontSize="18" IsEnabled="False"/>                   
        <ProgressBar DockPanel.Dock="Top" Minimum="0" Maximum="1"  IsIndeterminate="True"  Height="30"/>
    </StackPanel>
</UserControl>
